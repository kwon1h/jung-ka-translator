# 개발 가이드라인

이 문서는 중카 번역기 개발 전에 반드시 확인해야 하는 운영, 보안, 품질 기준이다. 새 기능을 추가할 때는 먼저 이 문서의 체크리스트를 통과해야 한다.

## 개발 전 체크리스트

- 게임 창 선택이 실제 캡처 대상과 일치하는지 확인한다.
- OCR 영역은 선택한 창 기준 상대 좌표로 저장하고, 창 이동/크기 변경 시 영역이 벗어나지 않게 한다.
- 오버레이가 OCR 캡처에 다시 잡히지 않도록 캡처 직전 숨김 또는 캡처 제외 처리를 유지한다.
- 같은 OCR 원문, 같은 사전 결과, 같은 유저의 유사 채팅은 API로 다시 보내지 않는다.
- 모든 번역 API 호출은 사전/필터/캐시를 통과한 뒤 마지막 단계에서만 수행한다.
- OCR 실패, 캡처 실패, 번역 실패는 서로 구분해서 로그와 UI 상태에 남긴다.
- UI 스레드 예외는 오류창을 반복 생성하지 않고 로그로 남긴다.
- 로그에 API 키, 토큰, 인증 헤더, 민감한 사용자 설정을 남기지 않는다.
- 문서와 소스는 UTF-8로 저장한다.

## 실패 사례와 재발 방지

### `paragraphWidth` 오류창 폭주

문제:
- 오버레이 텍스트 렌더링에서 유효하지 않은 폭이 들어가 WPF 예외가 반복 발생했다.
- 전역 예외 처리에서 MessageBox를 계속 띄워 앱 사용이 불가능해졌다.

방지:
- 텍스트 렌더링 폭/높이는 최소값을 보장한다.
- 루프 내부 예외는 상태와 로그로 보내고 MessageBox를 띄우지 않는다.

### 잘못된 창 선택

문제:
- 사용자가 번역기 창을 선택하고 게임 영역을 지정하면 OCR/캡처 대상이 어긋났다.

방지:
- 마지막으로 선택한 게임 창 이름과 프로세스를 저장한다.
- 시작 시 저장된 창을 자동 선택하고 없으면 PopKart/Kart 관련 창을 우선 탐색한다.
- 선택 창과 영역 지정 창이 다르면 명확한 경고를 제공한다.

### OCR/오버레이 자기 캡처

문제:
- 선택 영역 오버레이가 다시 OCR에 잡히면 번역 결과가 원문처럼 인식되는 피드백 루프가 생길 수 있다.

방지:
- 오버레이 창은 click-through, topmost, 캡처 제외 속성을 유지한다.
- 캡처 직전 오버레이를 숨기고 캡처 후 다시 보인다.
- 27인치 FHD 기본 프리셋은 검은 반투명 배경, 흰 글씨, 검은 외곽선, 22pt 이상으로 둔다.
- 오버레이 관련 변경 후에는 반드시 실제 채팅 영역에서 OCR 원문 로그를 확인한다.

### 유사 채팅 오탐

문제:
- 유저명이 같다는 이유만으로 다른 메시지가 유사 채팅으로 잘못 분류될 수 있다.

방지:
- 유사도 계산은 메시지 토큰 기준으로 한다.
- 유저명은 비교 범위를 좁히는 조건일 뿐, 유사 판정의 근거가 되어서는 안 된다.
- 필터 로그에는 speaker, message, score, overlap, union을 남긴다.

### 깨진 인코딩

문제:
- README, 사전, 상태 메시지가 깨진 문자열로 저장되면 사용자 안내와 디버깅이 불가능하다.

방지:
- 문서는 UTF-8로 저장한다.
- PowerShell 출력이 깨지는 것과 파일 자체 인코딩 문제를 구분한다.
- 문서 수정 후 `Get-Content -Encoding UTF8` 또는 에디터로 실제 표시를 확인한다.

### 사전 미적용으로 인한 API 낭비

문제:
- 게임 UI 고정어와 빠른 답장 문구가 API로 전송되면 비용과 지연이 증가한다.

방지:
- 고정 문구는 `UserDictionaryStore.DefaultDictionary`에 먼저 추가한다.
- 사전 100% 일치 문장은 DeepL로 보내지 않는다.
- 사전 부분 치환 후 중국어/일본어가 남지 않으면 DeepL로 보내지 않는다.

## 보안 기준

- API 키는 `ApiKeyStore`를 통해 Windows DPAPI CurrentUser 범위로 저장한다.
- API 키 원문은 로그, 예외 메시지, 진단 탭, README 예시에 남기지 않는다.
- `%LocalAppData%\GameOverlayTranslator` 아래 사용자 설정 파일은 Git에 포함하지 않는다.
- 네트워크 실패는 정상 실패 경로로 처리한다. 재시도 폭주를 만들지 않는다.
- DeepL 응답 오류는 사용자에게 상태로 알리고, 원문 또는 사전 치환 결과를 fallback으로 표시한다.
- 외부 API 추가 시 반드시 `ITranslationService` 구현으로 격리한다.

## 번역 품질 기준

- 게임 UI 고정어는 번역 API보다 사전이 우선이다.
- 채팅 빠른 답장, 트랙명, 모드명, 아이템명, 차량명은 가능한 한 사전에 추가한다.
- 사전 원문과 OCR 메시지가 정확히 일치하면 API 호출 없이 사전 번역어를 표시한다.
- 사전 부분 치환 후 원문 언어 문자가 남지 않으면 API 호출 없이 치환 결과를 표시해야 한다.
- OCR 품질이 낮은 문장은 번역하지 않고 필터링하거나 OCR 원문만 표시한다.
- 결과가 이전 결과를 완전히 포함하면 이전 결과를 제거해 겹침을 줄인다.

## 배포 기준

- 기본 배포물은 portable zip이다.
- 사용자는 .NET SDK를 설치하지 않아도 실행할 수 있어야 한다.
- README에는 Windows OCR 언어팩, DeepL 키, 창모드/전체창모드 조건을 명확히 적는다.
- 독점 전체 화면은 v1 지원 범위가 아님을 명시한다.
- 로그 위치와 문제 신고 시 필요한 정보가 문서화되어 있어야 한다.

## 완료된 기준

- 사전 부분 치환 후 중국어/일본어가 남지 않는 경우 DeepL 호출을 생략한다.
- 번역 요청량은 진단 로그와 세션 누적 표시로 확인한다.
- 사용자 사전은 카테고리별로 관리한다.
- 파서, 사전 카테고리, 사전 기반 API 생략 정책은 회귀 테스트로 검증한다.

## 지속 운영 항목

- 새 스크린샷을 받을 때마다 기본 사전 후보를 추가한다.
- OCR 오인식 사례는 수동 OCR 테스트와 회귀 테스트에 반영한다.
- 게임 패치로 UI 문구가 바뀌면 README와 기본 사전을 함께 갱신한다.
