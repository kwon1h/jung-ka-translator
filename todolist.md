[DONE] 1. 치명 오류 안정화
- `paragraphWidth` 오류를 수정한다.
- OCR/오버레이 루프에서 예외가 나도 오류창이 반복 생성되지 않게 한다.
- 예외는 로그와 상태 표시로만 남기고 세션은 가능한 범위에서 계속 동작하게 한다.

[DONE] 2. 깨진 한글 문자열 복구
- XAML/CS에 깨진 UI 문구를 정상 한글로 복구한다.
- 상태 메시지, 로그 메시지, 버튼/탭/라벨을 정상 한글로 정리한다.
- 로그 메시지 포맷을 원인 파악이 가능하게 통일한다.

[DONE] 3. 채팅 번역 v1 안정화
- 기본 모드를 `채팅 번역`으로 고정한다.
- `화면 번역`은 실험 기능으로 분리하거나 숨김 처리한다.
- 채팅 영역 OCR 결과만 파싱하고 UI 전체 텍스트는 번역 대상에서 제외한다.
- 유저명/메시지 분리 실패 케이스 로그를 강화한다.

[DONE] 4. 오버레이 모드 개선
- 선택한 게임 채팅 영역 위에 흰 테두리와 검은 글씨로 표시한다.
- 오버레이 투명도 조절을 추가한다.
- 오버레이가 마우스 클릭을 막지 않도록 click-through 처리한다.
- 오버레이가 OCR 캡처에 다시 잡히지 않도록 검증하고 필요하면 캡처 직전 숨김 처리한다.

[DONE] 5. 필터 알고리즘 정리
- 동일 문장 중복 제거를 안정화한다.
- 이전 문장이 다음 문장에 완전히 포함되면 이전 결과를 제거한다.
- 같은 유저명만으로 유사 채팅 판단하지 않도록 보장한다.
- 번역 품질이 낮은 OCR 결과는 번역하지 않거나 OCR 원문만 표시한다.
- 필터 판단마다 `rule`, `reason`, `score`, `speaker`, `message`를 로그에 남긴다.

[DONE] 6. 설정 저장 강화
- 마지막 선택 창 이름/프로세스를 저장한다.
- 마지막 선택 영역을 저장한다.
- OCR 언어, 표시 방식, 폰트 크기, 색상, 투명도를 저장한다.
- 앱 시작 시 저장된 창을 자동 선택하고 없으면 PopKart/Kart 관련 창을 자동 탐색한다.

[DONE] 7. 디버깅 도구 강화
- 현재 캡처 프리뷰 기능을 확인하거나 보강한다.
- 현재 OCR 원문 보기 기능을 확인하거나 보강한다.
- 필터 판정 로그 탭을 정리한다.
- OCR 텍스트를 수동 입력해 파서/필터/번역 결과를 테스트하는 기능을 확인하거나 보강한다.

[DONE] 8. 검증
- 빌드 통과를 확인한다.
- 채팅 영역 선택, OCR, 번역, 오버레이 표시 흐름을 검증한다.
- 번역창 닫기, 영역 재선택, 시작/정지 반복을 검증한다.
- 잘못된 창 선택 시 명확한 경고가 표시되는지 확인한다.
- 로그 파일로 원인 추적이 가능한지 확인한다.
